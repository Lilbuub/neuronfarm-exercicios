// Exercício 2: Crie uma função com o nome "concatena" que receba duas strings como parâmetros e retorne a concatenação das duas strings. Em seguida, chame a função e imprima o resultado na tela.

let concatena = {
    primeironome: 'Cachorro',
    segundonome: '-Quente'
}
console.log(concatena.primeironome + concatena.segundonome);
